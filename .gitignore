# Secrets / local credentials
service-account.json

# Python cache / build output
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Logs
*.log
debugLog.txt
api/log.txt

# Generated timetable/output data
db/
db/timetable-*.json
db/repeat-*.json
dbfolder/*.json
output.txt

# Node / tooling
# package-lock.json is intentionally NOT ignored: four GitHub Actions
# workflows (class-change-notify, exam-schedule-notify, showup-schedule-notify,
# sync-seating-plan) run `npm ci`, which refuses to run at all without a
# committed lockfile. Every recorded run of all four failed at that exact step
# for exactly that reason — see commit fixing this.
node_modules/

# Local editor / agent clutter
.vscode/
.claude/
.orchids/

service-account*.json

python/generate_timetable/runtime/
